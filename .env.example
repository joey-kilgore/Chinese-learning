# Copy this file to .env and fill in your key for local development.
# .env is gitignored — never commit a real API key.
# When shipping to users, the key comes from the Settings screen instead.
EXPO_PUBLIC_CLAUDE_API_KEY=
